import React from "react";
import { View, ScrollView, Text } from "react-native";

const Schedule = () => {
    return (
        <ScrollView>
            <View>
                <Text>
                    Schedule
                </Text>
            </View>
        </ScrollView>
    )
}

export default Schedule;